{
  "//": "Copy this file to secrets.json (same folder) and fill in real values. secrets.json is gitignored (matches the repo's existing 'secrets.json' rule) — never commit it.",
  "host": "your.vm.ip.or.hostname",
  "port": 22,
  "user": "root",
  "password": "",
  "remotePath": "/opt/medicalassistant/release"
}
