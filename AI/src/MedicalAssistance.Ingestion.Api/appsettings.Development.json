{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "Postgres": "Host=localhost;Port=5433;Database=ai_med;Username=postgres;Password=postgres"
  },
  "Authentication": {
    "//": "Local development only. Real secrets come from the estate's secret store; keep two entries here while rotating.",
    "ApiKeys": [ "local-development-key" ]
  },
  "//OpenAI": "Model names only — non-secret, so they live here. The ApiKey for each section comes from user-secrets (dotnet user-secrets set 'OpenAIChat:ApiKey' <key>), never this tracked file. A section activates only once its ApiKey is present. Embeddings MUST stay text-embedding-3-large: it is the only OpenAI model that emits 3072 dims, matching the fixed vector(3072) column. text-embedding-3-small (1536) would boot fine but fail on chunk insert.",
  "OpenAIChat": {
    "Model": "gpt-4.1-mini"
  },
  "OpenAIEmbeddings": {
    "Model": "text-embedding-3-large"
  }
}
