{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    },
    "Console": {
      "//": "Show scope state (the ingestion-run scope the worker opens) on the console, so a local run's log lines carry the IngestionId, DocumentType and PatientId (T35).",
      "IncludeScopes": true
    }
  },
  "AllowedHosts": "*"
}
